# Application Name
spring.application.name=backend

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://mysql-2d2f5f2f-francodellap-9018.c.aivencloud.com:12101/defaultdb?ssl-mode=REQUIRED
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_ERXzxyzGjjU2d-PbV-b
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA (optional, if using JPA/Hibernate)
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.enabled=true
spring.servlet.multipart.file-size-threshold=2MB

# JWT Configuration (Uncomment and set your values)
# jwt.secret=yourSecretKeyHere
# jwt.expiration=3600000

# Spring DataSource Exclusion (if needed)
# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
# Enable static resource handling
spring.web.resources.add-mappings=true
spring.web.resources.static-locations=classpath:/static/

# Enable live reload
spring.devtools.livereload.enabled=true

# Thymeleaf Configuration
spring.thymeleaf.cache=false
spring.thymeleaf.enabled=true
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html




# Static resources
# Static resources
# Uncomment if using JWT
# jwt.secret=${JWT_SECRET}
# jwt.expiration=${JWT_EXPIRATION}
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration